{
  "name": "clay",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "start": "node server.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
